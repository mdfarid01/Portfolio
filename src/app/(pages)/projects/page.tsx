import ProjectsList from "@/components/sections/Projects/ProjectsPage";

// Route wrapper: export a simple page (server component) that renders the reusable ProjectsList.
export default function ProjectsPageRoute() {
  return <ProjectsList />;
}
